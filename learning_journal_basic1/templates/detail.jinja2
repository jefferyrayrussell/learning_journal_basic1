{% extends 'index.jinja2' %}
{% block body %}
    <article>
        <h1>{{ entry.title }}</h1>
        <h3>{{ entry.date }}</h3>
        <p>{{ entry.body }}</p>
        <a href="{{request.route_url('edit_view', id=entry.id) }}">Edit This Entry</a>
      </article>
{% endblock %}

